using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlobalVars : MonoBehaviour
{

    public static bool ShovelBool;
    // Start is called before the first frame update
    void Start()
    {
        ShovelBool = false;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
